let add = \x y. x + y in
  add 2 3

(\add.add 2 3) (\x y. x + y)

let add = \x y. x + y in
let sub = \x y. x - y in
  add 2 (sub 5 2)

(\add.\sub.add 2 (sub 5 2)) (\x y. x + y) (\x y. x - y)

(\add.add 2 ((\sub.sub 5 2) (\x y. x - y))) (\x y. x + y)
